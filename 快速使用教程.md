# Hysteria 2 快速使用教程

## 🎯 服务器已配置完成

- **服务器**: 23.95.14.139:443
- **密码**: BJOtbcXF1n563W7u7w_hy2
- **状态**: ✅ 已优化并运行中

---

## 📱 客户端使用方法

### 方法一：Mac 本地使用（推荐）

#### 1. 下载客户端

```bash
cd ~/Downloads
curl -Lo hysteria https://github.com/apernet/hysteria/releases/latest/download/hysteria-darwin-amd64
chmod +x hysteria
```

#### 2. 使用优化配置文件

将 `客户端优化配置.yaml` 复制到 Downloads 目录：

```bash
cp /Users/liubei/Desktop/emby/客户端优化配置.yaml ~/Downloads/config.yaml
```

#### 3. 启动客户端

```bash
cd ~/Downloads
./hysteria -c config.yaml
```

看到 `connected to server` 表示连接成功！

#### 4. 配置系统代理

**方法 A：使用 SOCKS5 代理（推荐）**
- 打开 系统设置 → 网络
- 选择当前网络 → 详细信息
- 代理 → SOCKS 代理
- 服务器：127.0.0.1
- 端口：1080
- 勾选 SOCKS 代理

**方法 B：使用 HTTP 代理**
- 同上，选择 HTTP 代理
- 服务器：127.0.0.1
- 端口：8080

#### 5. 测试连接

打开浏览器访问：
```
https://www.google.com
https://www.youtube.com
```

---

### 方法二：使用分享链接（手机/其他客户端）

#### 分享链接
```
hysteria2://BJOtbcXF1n563W7u7w_hy2@23.95.14.139:443/?insecure=1&sni=www.bing.com#Hysteria2
```

#### Android (SagerNet)

1. **下载 SagerNet**
   - GitHub: https://github.com/SagerNet/SagerNet/releases
   - 下载最新 APK 安装

2. **导入配置**
   - 打开 SagerNet
   - 点击右上角 ➕
   - 选择「从剪贴板导入」
   - 粘贴上面的分享链接

3. **连接**
   - 点击配置
   - 点击底部连接按钮
   - 允许 VPN 权限

#### iOS (Shadowrocket)

1. **购买 Shadowrocket**
   - App Store 美区账号
   - 价格：$2.99

2. **添加配置**
   - 打开 Shadowrocket
   - 右上角 ➕
   - 类型：Hysteria 2
   - 服务器：23.95.14.139
   - 端口：443
   - 密码：BJOtbcXF1n563W7u7w_hy2
   - SNI：www.bing.com
   - 允许不安全：开启

3. **连接**
   - 选择配置
   - 打开连接开关

---

### 方法三：Windows 使用

#### 1. 下载客户端
- 访问：https://github.com/apernet/hysteria/releases
- 下载：`hysteria-windows-amd64.exe`

#### 2. 创建配置文件

在同一目录创建 `config.yaml`，内容：
```yaml
server: 23.95.14.139:443
auth: BJOtbcXF1n563W7u7w_hy2
tls:
  sni: www.bing.com
  insecure: true
bandwidth:
  up: 100 mbps
  down: 500 mbps
fastOpen: true
lazy: false
socks5:
  listen: 127.0.0.1:1080
http:
  listen: 127.0.0.1:8080
```

#### 3. 运行

双击运行或命令行：
```cmd
hysteria-windows-amd64.exe -c config.yaml
```

#### 4. 配置系统代理

- 设置 → 网络和Internet → 代理
- 手动设置代理
- 地址：127.0.0.1
- 端口：8080
- 保存

---

## 🌐 浏览器配置（推荐）

### Chrome/Edge - 使用 SwitchyOmega

#### 1. 安装插件
- Chrome 应用商店搜索：SwitchyOmega
- 或访问：https://github.com/FelisCatus/SwitchyOmega/releases

#### 2. 配置代理

1. 点击插件图标 → 选项
2. 新建情景模式 → 代理服务器
3. 填写：
   - 代理协议：SOCKS5
   - 代理服务器：127.0.0.1
   - 代理端口：1080
4. 保存

#### 3. 使用

点击插件图标，选择刚创建的代理模式即可。

### Firefox

1. 设置 → 网络设置 → 手动代理配置
2. SOCKS Host：127.0.0.1
3. Port：1080
4. SOCKS v5：勾选
5. 确定

---

## ⚙️ 配置文件说明

### 带宽设置（重要！）

```yaml
bandwidth:
  up: 100 mbps      # 你的上传带宽
  down: 500 mbps    # 你的下载带宽
```

**如何设置？**

1. **测试你的实际带宽**
   ```bash
   # 访问 https://fast.com 或 https://speedtest.net
   ```

2. **设置为实际带宽的 50-80%**
   - 100M 宽带 → `up: 50 mbps, down: 200 mbps`
   - 200M 宽带 → `up: 100 mbps, down: 500 mbps`
   - 500M 宽带 → `up: 200 mbps, down: 1 gbps`
   - 1000M 宽带 → `up: 500 mbps, down: 2 gbps`

**注意**：设置过高会导致速度变慢！

### 代理端口

```yaml
socks5:
  listen: 127.0.0.1:1080    # SOCKS5 端口

http:
  listen: 127.0.0.1:8080    # HTTP 端口
```

可以修改端口避免冲突。

---

## 🧪 测试和验证

### 1. 检查连接状态

客户端运行后应该看到：
```
INFO  server mode
INFO  connected to server
```

### 2. 测试代理

```bash
# 测试 SOCKS5
curl -x socks5://127.0.0.1:1080 https://www.google.com

# 测试 HTTP
curl -x http://127.0.0.1:8080 https://www.google.com

# 查看 IP
curl -x socks5://127.0.0.1:1080 https://api.ip.sb/ip
```

应该显示服务器 IP：23.95.14.139

### 3. 速度测试

```bash
# 下载测试文件
curl -x socks5://127.0.0.1:1080 -O http://speedtest.tele2.net/100MB.zip

# 或访问
https://fast.com
```

---

## 🎮 不同场景使用

### 场景 1：日常浏览

**配置**：
```yaml
bandwidth:
  up: 50 mbps
  down: 200 mbps
```

**使用**：浏览器配置代理即可

### 场景 2：观看视频

**配置**：
```yaml
bandwidth:
  up: 100 mbps
  down: 500 mbps
```

**使用**：全局代理或浏览器代理

### 场景 3：下载文件

**配置**：
```yaml
bandwidth:
  up: 200 mbps
  down: 1 gbps
```

**使用**：下载工具配置代理
- 迅雷：设置 → 代理 → SOCKS5 → 127.0.0.1:1080
- IDM：选项 → 代理 → SOCKS5 → 127.0.0.1:1080

### 场景 4：游戏加速

**配置**：
```yaml
bandwidth:
  up: 50 mbps
  down: 100 mbps

fastOpen: true
lazy: false
```

**使用**：
- Steam：设置 → 下载 → 代理服务器
- Epic：设置 → 连接 → 代理

---

## 🔧 常见问题

### Q1: 连接不上？

**检查清单**：
```bash
# 1. 检查客户端是否运行
ps aux | grep hysteria

# 2. 检查端口是否监听
lsof -i :1080
lsof -i :8080

# 3. 测试服务器连接
ping 23.95.14.139

# 4. 查看客户端日志
./hysteria -c config.yaml
```

### Q2: 速度慢？

**解决方法**：
1. 降低带宽设置（设置过高会变慢）
2. 检查本地网络
3. 更换时间段测试

### Q3: 浏览器无法访问？

**检查**：
1. 代理是否正确配置
2. 客户端是否在运行
3. 尝试关闭浏览器重新打开

### Q4: 如何关闭代理？

**Mac**：
- 系统设置 → 网络 → 代理 → 取消勾选

**Windows**：
- 设置 → 代理 → 关闭

**浏览器**：
- SwitchyOmega：选择「系统代理」或「直接连接」

---

## 📝 快速命令参考

### Mac 使用

```bash
# 启动客户端
cd ~/Downloads
./hysteria -c config.yaml

# 后台运行
nohup ./hysteria -c config.yaml > hysteria.log 2>&1 &

# 查看日志
tail -f hysteria.log

# 停止
pkill hysteria
```

### 服务器管理

```bash
# 连接服务器
ssh root@23.95.14.139

# 查看状态
systemctl status hysteria-server

# 重启服务
systemctl restart hysteria-server

# 查看日志
journalctl -u hysteria-server -f
```

---

## 🎯 推荐使用流程

### 第一次使用

1. ✅ 下载客户端
2. ✅ 复制配置文件
3. ✅ 调整带宽设置
4. ✅ 启动客户端
5. ✅ 配置浏览器代理
6. ✅ 测试访问

### 日常使用

1. 启动客户端：`./hysteria -c config.yaml`
2. 浏览器切换到代理模式
3. 使用完毕关闭客户端

---

## 💡 使用技巧

### 1. 自动启动（Mac）

创建启动脚本：
```bash
cat > ~/start-hysteria.sh <<'EOF'
#!/bin/bash
cd ~/Downloads
./hysteria -c config.yaml
EOF

chmod +x ~/start-hysteria.sh
```

使用：`~/start-hysteria.sh`

### 2. 开机自启（Mac）

创建 LaunchAgent：
```bash
cat > ~/Library/LaunchAgents/com.hysteria.plist <<'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.hysteria</string>
    <key>ProgramArguments</key>
    <array>
        <string>/Users/liubei/Downloads/hysteria</string>
        <string>-c</string>
        <string>/Users/liubei/Downloads/config.yaml</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
</dict>
</plist>
EOF

launchctl load ~/Library/LaunchAgents/com.hysteria.plist
```

### 3. 规则分流

只代理特定网站，国内网站直连：

使用 SwitchyOmega 的自动切换模式，添加规则。

---

## 📊 性能监控

### 查看实时速度

客户端日志会显示：
- 上传/下载速度
- 延迟
- 丢包率

### 流量统计

使用系统工具：
```bash
# Mac
nettop

# 或安装 iftop
brew install iftop
sudo iftop -i en0
```

---

## 🎉 开始使用

现在你可以：

1. **Mac 用户**：
   ```bash
   cd ~/Downloads
   curl -Lo hysteria https://github.com/apernet/hysteria/releases/latest/download/hysteria-darwin-amd64
   chmod +x hysteria
   cp /Users/liubei/Desktop/emby/客户端优化配置.yaml ./config.yaml
   ./hysteria -c config.yaml
   ```

2. **配置浏览器代理**：SOCKS5 → 127.0.0.1:1080

3. **开始使用**：访问 Google、YouTube 等

**享受高速网络！** 🚀
